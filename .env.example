# PostgreSQL connection string used by Prisma.
# Recommended: a serverless managed Postgres (Neon or Supabase) for the 24/7 cron pipeline.
DATABASE_URL="postgresql://user:password@localhost:5432/pulseagent?schema=public"

# LLM inference via OpenRouter (OpenAI-compatible). Uses free Poolside models.
OPENROUTER_API_KEY="sk-or-v1-..."

# External data source keys
NEWS_API_KEY=""
GOOGLE_SEARCH_API_KEY=""
GOOGLE_SEARCH_CX=""

# Reddit username used to build the public .json User-Agent (no OAuth required).
REDDIT_USER="YourRedditUser"
